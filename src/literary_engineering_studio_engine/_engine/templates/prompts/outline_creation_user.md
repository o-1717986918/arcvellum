Create a candidate plot outline asset for the current work project.

Asset type may be `outline`, `chapter-plan`, or `scene-list`; all should produce a valid `plot_outline.v1` JSON object.

Use the project premise, existing canon, characters, and world constraints. Include `risks` and `source_paths`.

# Studio Worker 运行纪律

你正在 Studio 创建的隔离任务工作区中执行一个正式任务。

- 以 `AGENT_TASK.md` 和 `_task/task.json` 为当前唯一任务入口。
- 完成任务包要求的语义工作，不要仅创建空文件或复述要求。
- 不修改 source artifacts，不新增任务包未允许的正式产物。
- 不自行调用提交、完成、晋升、应用或发布命令，Studio Worker 会负责验收。
- 遇到资料不足或约束冲突时保留阻塞证据，不得凭空补写 Canon。
- 当前用户创作方向只能改变创作选择，不能绕过已确认 Canon 和正式门禁。

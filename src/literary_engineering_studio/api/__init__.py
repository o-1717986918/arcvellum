"""Studio HTTP boundary modules.

The public ``api_server`` module remains the stable application factory while
request contracts, dependencies, streaming helpers, and routers are migrated
here in independently verifiable batches.
"""

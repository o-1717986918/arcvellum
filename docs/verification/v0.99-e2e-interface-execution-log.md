# ArcVellum v0.99 真实创作与接口化执行日志

状态：执行中  
基线提交：`75fcb8d`  
目标：以普通用户的全自动路径完成两章、六场、不少于 30000 个中文内容字符的正式作品，并在保持该证据有效的前提下完成 v0.99 模块化收敛。

## 1. 不可替代的验收证据

真实闭环必须同时证明：

1. 作品通过公开 Studio API 创建并记录创作方向；
2. 使用内置 `pi-worker` 和用户已配置的模型连接启动 `full_auto`；
3. 长篇规划物化两章、六场正式合同；
4. 每场正文均经过候选生成、独立审查、晋升、状态写回和连续性写回；
5. Reader 投影的正式正文不少于 30000 个中文内容字符；
6. Autopilot 正式完成，而不是测试脚本提前暂停；
7. `export-and-release` 无阻断并生成至少一个可下载正式文件；
8. 运行记录包含真实 Provider 请求与 Pi Worker 会话。

原有 `first-loop` 验收继续保留，用于快速验证第一场到下一场的衔接；它不能代替本轮 `full-work` 验收。

## 2. GitHub Issue #20 归因

Issue 截图暴露的是跨模块产品故障，不是一条错误文案：

| 现象 | 责任模块 | 必须修复的根因 |
|---|---|---|
| Prompt `67021 > 48000` | Prompt Program / Evidence Projection | 编译前预算、按任务裁剪和可行动的超限诊断 |
| `repair timeout` | Runtime Provider Reliability / Repair | 独立 repair timeout、错误分类和有限恢复 |
| `no-progress guard stopped...` | Pi Worker / Autopilot Progress | 区分模型无产物、重复工具错误和正式项目无进展 |
| sidecar 提醒重复刷屏 | Advisor Inbox / Notification Deduplication | 以阻断身份和 revision 去重，状态变化后再提醒 |
| 用户不知道如何补设定 | Recovery Guidance / Onboarding | 错误卡给出中文原因、影响、系统可自动做什么和用户下一步 |

内部异常原文保留在诊断日志和高级观测中；普通界面只展示稳定错误代码、中文说明和可执行动作。

## 3. 实施纪律

- 每批先定位模块和公开接口，不跨模块自由修改；
- 每批保留输入、输出、错误语义和回滚点；
- 不以减少文学 Gate、缩短目标字数、放宽审查或伪造交付换取吞吐；
- 允许改变内核流程，但必须先证明新流程保留正式事实和端到端验收；
- 每个批次更新本文，执行定向测试、架构审计并创建独立 Git 提交。

## 4. 批次状态

- [x] E0：将连续 E2E 验收扩展为 `first-loop` 与 `full-work` 双模式。
- [ ] E1：运行两章三万字基线并记录所有阻断。
- [ ] E2：修复 Issue #20 的错误恢复与设定引导。
- [ ] M1-M3：组合根、Runtime/Provider、Literary IR/Prompt Program。
- [ ] M4-M6：Engine Public API、持久化/事件端口、Vue Feature Ports。
- [ ] M7：领域热点偿债与完整回归。
- [ ] D1：模块说明、面向接口 Agent 开发标准、最终用户闭环与交付。

## 5. E2 批次计划：故障呈现与恢复合同

本批只改“错误如何跨模块流动”，不改文学 Gate、任务顺序或正式写回规则：

1. 在应用层建立版本化 `FailurePresentation`，统一稳定代码、类别、用户标题、中文说明、影响、恢复动作与诊断原文；
2. Runtime/Prompt/Autopilot 的原始错误先分类，再将用户可读投影写入运行状态；诊断事件继续保留原文；
3. Autopilot 状态 API 同时保留兼容字段 `last_error`，并新增结构化 `failure`，避免前端继续猜测字符串；
4. 顾问 Inbox 以 `failure.code + route + task/revision` 去重，不再因 sidecar 数量或原始措辞变化重复刷屏；
5. 前端以恢复卡展示原因、影响和下一步；高级详情可以展开技术诊断；
6. 为 Issue #20 的 Prompt 超限、repair timeout、no-progress 和 sidecar 阻断建立回归测试。

完成标准：普通界面不再直接显示 Issue #20 截图中的英文内部异常；同一阻断不会重复制造提醒；已有 API/客户端兼容测试继续通过。

### E2a 实施记录（2026-08-20）

- 新增 `arcvellum/failure-presentation/v1`，将 Prompt 超限、修订超时、Agent 空转、预检失败、工作流缺证据、修订来源过期、模型授权和额度问题映射为稳定产品语义；
- Autopilot 状态与 SSE 使用同一投影，兼容字段 `last_error` 返回中文摘要，原始异常仅保留在折叠技术详情；
- 顾问提醒改用稳定 gate/failure 身份，并在读取时折叠旧版本按原始措辞产生的重复记录；
- 推进面板新增恢复卡，显示原因、影响、动作和可折叠技术诊断；
- 39 项 Python 定向测试、4 项 Vue 组件测试、客户端构建与架构审计通过；架构棘轮曾拦截新增复杂度，已通过提取 API/Inbox 用例函数消除增量债务。

E2 尚未结束：下一批处理 Prompt 编译前预算与自动证据降级、repair timeout/no-progress 的有界恢复，以及“如何补全作品设定”的上下文帮助。

## 6. E2b 批次计划：Pi Worker 多产物落地协议

真实基线在 `character-and-world-assets-world-foundation-asset-review-pass` 暴露了一个可复现的运行时协议缺陷：模型已经提交四个 Agent-owned 产物，但首轮超长批量工具参数被 Provider 截断，其中世界规则 JSON 留下语法错误；随后模型在同一响应中逐文件调用四次 `validate_output`，首个校验改变了全局协议阶段，导致其余三个兄弟调用被错误拒绝，并在第六轮耗尽预算。

本批按 Runtime 模块边界修复，不增加轮次来掩盖问题：

1. `write_expected_output` 每次写入后自动运行全量本地校验，并把聚合错误返回模型；
2. `validate_output(path)` 可以返回单文件结果，但 Worker 的内部状态始终保留全量聚合结果，避免最后一个通过的单文件覆盖其他失败；
3. 一次 Provider 响应开始时冻结该响应的 required-tool lease，同一响应中的兄弟工具调用不因前一调用改变状态而被误拒绝；
4. 多产物写入提示明确限制超长 batch，优先按一个完整产物一次提交，避免工具参数在 Token 上限处被截断；
5. 增加“截断 JSON + 多文件 + 最后一轮修复”的 Worker 回归测试，再恢复真实 E2E 基线验证。

完成标准：相同四产物任务在一个 JSON 语法错误时得到精确修复机会；无效兄弟调用不消耗为协议冲突；全部产物本地有效后仍由 Studio 正式预检裁决。

### E2b 实施记录（2026-08-20）

- 写入工具现在立即返回全量聚合校验，截断 JSON、缺失兄弟产物和提交来源问题都能在同一轮被模型看到；
- 单文件 `validate_output` 不再用局部通过结果覆盖全局失败状态；
- required-tool 改为 Provider 响应级 lease，同一响应中的兄弟调用不会因先执行调用改变状态而被误判为越权；
- 主创 Worker 取消“所有产物必须首轮批量提交”的危险指令，超过 12000 字符的多产物任务按完整文件逐项提交；
- Pi Worker 46 项测试、Python Runtime/合同 24 项测试及架构审计通过，未增加依赖违规或循环。

真实 E2E 复验尚未完成；本批提交后恢复原运行，只有穿过原 `world-foundation` 修订门禁才算关闭该阻断。

## 7. E2c 批次计划：审查 Agent 的读取到提交收敛

E2b 后原 `asset-review-pass` 已完成并进入新一轮独立 `asset-review-agent-task`，证明四产物修订阻断被解除。随后审查 Agent 连续两轮调用 `read_task_context`：第一轮实际完成了长篇语义分析，第二轮又重新读取同一合同；由于合同读取没有进入 Worker 进度指纹，no-progress guard 将其误判为两个完全相同的空转回合。

本批修复：

1. 将任务合同读取次数纳入 Worker 状态和进度摘要；
2. 非正文 Agent 保留一个证据读取响应，一旦读取任务合同或任一按需资料，下一 Provider 回合强制进入 `write_expected_output`；
3. required-tool 阶段误用改为可纠正的非终止工具错误，避免供应商忽略 tool choice 时直接结束会话；
4. 保持 Studio 正式预检、独立审查身份和失败闭锁不变；
5. 以审查 Agent “读取一次后必须写入”的单元测试和原项目恢复运行验证闭环。

### E2c 实施记录（2026-08-20）

- `taskContextReads` 成为显式 Worker 状态并进入进度摘要，读取任务合同不再被误判为零进展；
- 审查/分析 Agent 可以先完成一个读取响应，但读取后下一回合由协议强制进入产物写入；
- 阶段误用不再终止整个会话，供应商忽略 tool choice 时可在下回合纠正；
- Pi Worker 构建与 47 项测试通过。真实项目已完成此前失败的 `asset-review-pass`，当前复验目标转为独立 `asset-review-agent-task`。

## 8. M1 批次计划：唯一应用组合根

实际代码复核确认 M1 尚未实现：`api_server.create_app()` 直接构造 Lifecycle、Bootstrap、Advisor 与 Autopilot；`ApplicationLifecycleManager` 直接构造 JobStore、事件总线、缓存、进程管理器、Runtime Pool、Coordinator 与 Supervisor；`_STYLE_MOUNTS` 是跨 App 共享的模块级可变服务。

本批边界：

1. 新建 application-owned ports，接口仅覆盖生命周期真正使用的能力；
2. 新建 infrastructure 默认适配器装配，保持安装版路径、线程数、缓存和 Runtime 行为不变；
3. 新建 `ApplicationContainer`，统一持有 config、ports 与 lifecycle/bootstrap/advisor/autopilot/style services；
4. `ApplicationLifecycleManager` 只接收 ports，不再导入或构造具体基础设施；
5. `create_app(config_override=None, container=None)` 只消费 container 并装配 HTTP 路由；
6. 删除 `_STYLE_MOUNTS`，每个 App 拥有独立 Style 服务和 Cache；
7. 保留旧 `literary_engineering_studio.lifecycle.ApplicationLifecycleManager(config)` 兼容入口，但兼容装配不进入 application 层；
8. 增加双 TestClient 隔离与内存/替身 port 测试，执行 API、生命周期、Style、架构审计和全量必要回归。

完成标准：`application/lifecycle.py` 不依赖 concrete infrastructure；两个 App 不共享 Style/Cache；默认桌面与开发服务器行为不变。

### M1 实施记录（2026-08-20）

- 新增 `ApplicationPorts`、`ApplicationServices` 与 `ApplicationContainer`，应用服务只消费端口合同；
- 默认基础设施装配集中在 `infrastructure/defaults.py`，组合根集中在 `infrastructure/composition.py`；
- 旧 Lifecycle 入口降为兼容门面，具体装配留在 infrastructure，未把基础设施依赖倒灌回 application；
- `create_app()` 支持显式注入 container，Style Mount 和 Cache 改为应用实例隔离；
- Reader Contract 的 Studio 扩展预检经 `preflight/service.py` 组合，不让核心 canonicalization 反向依赖具体文学适配器；
- 架构审计、依赖方向及 122 项相关合同/运行时/API 测试通过。测试命令显式绑定当前工作树 `src`，防止 editable install 指向旧仓库产生伪通过。

### E2d Reader Contract 收敛记录（2026-08-20）

- 真实运行在 `scene_0001` 暴露了章节读者合同提示超过 48k 上限：任务把全项目场景和冗余 sidecar 当成主 Agent 必读资料；
- Agent 阅读集已收敛为当前章节场景、项目目标、字数预算和当前章节 scaffold，实测输入降至约 8.4k tokens；
- 所有 `.agent_tasks.md` 统一归 Studio 所有，不再允许 Agent 改写 CLI sidecar；
- 章节义务 JSON 成为唯一 Agent 权威输出；schema、chapter id、场景集合、计数单位和输出路径由系统锁定，Markdown 镜像与生命周期证据由 Studio 生成；
- 预检要求覆盖当前章节全部计划场景及完整读者体验字段，不能以单场数据伪装整章合同；
- Reader Contract 修复与 M1 组合根相关的 122 项测试及架构审计通过。下一证据仍是恢复真实全自动运行并进入正文生成链路。

### E2e 批次计划：正文联合门禁收敛

真实 `scene_0001` 正文已生成，但六轮增量修订在 Style Lint 与字数预算之间来回振荡：删除逗号链和抽象总结后跌破字数下限，补足字数又重新引入同类句法。该失败属于 Repair Coordinator 缺少跨回合稳定性合同，不归因于模型“不会写”。本批执行：

1. Repair Coordinator 记录同一任务已出现过的 Gate 类别，不因某项暂时通过就忘掉；
2. 正文修订只要曾触发 Style 或字数问题，后续提示始终同时携带目标/上下限与精简 Style 回归防线；
3. 保持 Style Lint 和字数 Gate 的硬度，不用降级 medium 规则或增加盲目重试换取通过；
4. 增加“首轮 Style+字数、次轮仅字数”的跨回合测试，证明第二轮仍被要求防止 Style 回归；
5. 恢复同一真实项目，要求正文通过预检、进入 AgentReview 并完成正式晋升。

### E2f 批次记录：AgentReview Canon 状态归一化

联合 Style/字数修订后，`scene_0001` 正文以 4684 个中文内容字符通过确定性预检并进入独立 AgentReview。审查 Agent 正确保留了“是否形成持续 Canon 事实仍未知”的文学判断，但另写了非合同状态 `declared`；Studio 预检未拦截该嵌套状态，直到 Engine Core Gate 导入后才回滚事务。

本批按所有权边界修复：

1. `canon_writeback.canon_change` 继续由 Agent 判断；
2. `canon_writeback.status` 改由 Studio 从该判断确定性派生：`false -> not_required`、`true -> pending_canon_evolve`、`unknown -> unknown`；
3. 不把非法枚举送进 Engine，也不消耗额外模型轮次修正机械状态词；
4. 增加 `declared + unknown` 的真实失败形态回归，并执行 Task Preflight、Scene Review/Revision、正文晋升共 55 项测试；
5. 架构审计通过，无新增依赖违规、循环或债务突破。

下一验收点仍是同一真实运行完成 AgentReview 写回、候选晋升、状态/Canon/连续性闭环，再进入 `scene_0002`。

### E2g 批次记录：审查嵌套合同前移与角色别名归一化

Canon 状态修复后，真实 AgentReview 已成功写回，但 Engine 发现 `new_character_register` 使用了 `character/type/waiver` 等自然别名：正式主角“林桓”被误投影为临时新角色 `#1`，从而生成了不应存在的修订方向卡。该问题再次证明 Studio 预检只检查顶层 schema、未复用 Engine 嵌套 Gate，会把可恢复的传输问题拖到写回之后。

本批修复：

1. Scene Review canonicalizer 将 `character -> name`、`waiver -> waiver_reason`、明确的 `type -> persistence` 做无损归一化；
2. 对能从 `characters/*.yaml` 精确匹配的人物写入 `already_in_characters/formal_character_path`，不让正式人物重复进入新角色流程；
3. Engine 通过既有 review semantics 入口公开正式的新角色审查，Studio 预检复用该 Gate，未解决问题会在 Agent repair 阶段出现，不再等 Core Gate 回滚；
4. 架构棘轮曾拒绝新增 Engine 内部依赖与两个复杂度越界，最终实现复用既有依赖入口并拆小函数，审计恢复通过；
5. 55 项场景预检/审查/修订/晋升回归通过。

下一步恢复同一任务重新审查；clean pass 必须直接进入 promotion，不能因非阻塞观察或传输别名制造 revision loop。

### E2h 批次记录：资产审查响应 envelope 归一化

`scene_0001` 已完成正文晋升、静态审查、状态/Canon/连续性闭环并进入 `scene_0002`。随后独立资产审查给出了完整且可用的文学判断，但把 `status/checked/blocking_issues/warnings/revision_actions/promotion_risks/conclusion` 包在通用 `review` 对象中；预检只接受平铺合同，Pi repair 又无法产生确定性进展，造成一次不必要的停止。

本批按传输层所有权修复：

1. 新增独立 `preflight.asset_review_metadata`，只展开无冲突的已知 Agent 审查字段；
2. candidate identity、schema、digest 等机器字段仍由既有系统合同覆盖，不接受 Agent 自行决定；
3. 顶层与嵌套判断冲突时拒绝静默覆盖，继续交给正式预检暴露；
4. 将裸字段 revision target 归一化一并迁入该高内聚模块，`canonicalization.py` 从 884 行降至 799 行，没有抬高冻结基线；
5. 真实失败形态回归证明 canonicalize 后可直接通过完整 `validate_task_outputs`；资产预检/修订/晋升共 48 项测试及架构审计通过。

下一验收点：重启当前开发服务并恢复同一运行，资产审查必须不经模型 repair 直接写回，继续完成 `scene_0002` 正式正文闭环。

### E2i 批次记录：正文预算收敛与 Prompt 标准所有权

同一真实运行已完成 `scene_0002` 的 RP、分支、composition、正文、一次定向修订、复审、晋升、状态/Canon/连续性闭环，并进入 `scene_0003`。第三场初稿未达到 4050 字下限；六轮修订从 1696 单调增长到 3948 个中文内容字符，却在距下限 102 字时耗尽固定 repair 预算。同时 candidate manifest 以字符串 `system` 占住了本应由受保护 prompt 写入的 creative-quality digest 和 reader contract。

本批修复：

1. prompt manifest 中的 creative-quality digest、reader-experience contract 与 narrative-rhythm contract 明确归 Studio 所有，canonicalizer 总是按受保护来源覆盖占位符或错误值；
2. 正文首轮约束新增“按 composition 节拍分配篇幅、不得因情节讲完提前结束”的前置自检；
3. 大幅欠字仍要求按事件链完整扩写；距下限不超过 8%/200 字时切换为保留已通过正文的净增量策略，避免每次从头重写；
4. Prose 的 bounded repair 上限由 6 调为 8；确定性摘要无变化时仍立即 no-progress 停止，因此不会把机械失败变成无限重试；
5. 77 项 Prompt/Preflight/Execution Profile/Repair Context 测试与架构审计通过。

下一验收点：恢复 `scene_0003`，候选系统字段应一次归一化，正文应在有限修订内同时满足 Style 与 4050-4950 中文内容字符合同。

### E2j 批次记录：语义修订事务作用域

`scene_0003` 候选已满足正文预算并完成 AgentReview 与晋升，但静态审查后的修订任务在第二次 repair 停止。预检同时发现反规避清单中的 `revised_excerpt` 未实际出现在修订正文，以及风险状态与文学判断矛盾；旧作用域只按问题路径开放 JSON、保护 Markdown，模型无法兑现它在清单中声明的正文修改。

本批按事务边界修复：

1. 新增独立 `runtime.repair_scope`，集中管理 Agent 可写产物、问题目标和语义依赖扩展；
2. `candidate-revision/static-revision` 遇到 `scene-revision-invalid` 时，正文候选与修订清单始终作为一个可写事务，不能只改清单宣称正文已修改；
3. 修订报告和其余已通过产物继续受快照保护，不把联动修复退化成全产物重写；
4. 不降低反规避、字数或静态审查门禁，也不手工修改真实项目产物；
5. 59 项增量修复、Task Preflight、Scene Revision 回归与架构审计通过。

恢复验证又证明联动条件不能只在 `scene-revision-invalid` 出现后触发：静态修订初稿先因字数不足而扩写正文，既有反规避清单随即过期。作用域因此进一步收敛为：修订状态下，任何指向正文候选或清单的失败都联动两者；Pi Worker 已有的 `not_submitted_this_run` 合同会强制本轮逐一提交这两个目标。新增真实字数失败形态回归后，60 项 Python 测试、Pi Worker 47 项测试与架构审计通过。

真实恢复期间，DeepSeek 连续返回精确的 `Request timed out.`。该错误原先未进入传输故障标记，因而被错误分类为不可重试 `model_error` 并立即要求人工介入。错误分类现将 provider request timeout 归入 `transient_network`，由既有恢复阶梯执行有界重试、session renew 与证据化停止；额度和鉴权错误仍保持不可重试。49 项 Pi/Autopilot/OpenCode 故障回归与架构审计通过。

下一验收点：恢复同一真实运行；静态修订必须完成正文与清单的联合修正，通过复核并进入 `scene_0003` 状态/Canon/连续性闭环。

### E2k 批次记录：修订证据分类归一化

`scene_0003` 的静态修订已同时扩写正文并修正真实对照句，但模型又把 `candidate-word-budget-invalid` 的修复摘要写进 `anti_evasion_rows`。该行并非文学反规避判断，且摘要不是正文逐字引证，exact-evidence Gate 因而正确拒绝；问题属于结构化分类传输，不应通过放宽引证门禁解决。

本批按确定性与文学判断所有权修复：

1. Scene Revision canonicalizer 只识别以正式问题码 `candidate-word-budget-invalid` 开头的误分类行，并将其移出反规避证据表；
2. 真实机械对照、换皮转折及未知问题行继续原样保留并接受 exact-source / exact-revision 校验；
3. 不补写或模糊匹配引证，不伪造 Agent 文学判断，也不降低字数、Style 或静态审查门禁；
4. 回归测试复刻“一个有效反规避行 + 一个误分类字数行”，证明仅后者被移除；
5. 61 项场景修订、Task Preflight、增量修复测试与架构审计通过。

下一验收点：重启开发服务并恢复原运行；该任务应直接通过分类归一化和正式预检，完成 `scene_0003` 后处理并进入第二章。

### E2l 批次记录：长场景抽象表达门禁校准

分类归一化已在真实运行中生效，exact-evidence 两项错误消失。后续唯一阻断来自 `abstract-summary-density`：旧实现按全文中 `答案、真相、本身、像是` 等词的绝对总数达到 8 次即阻断，不按场景长度归一化，还把比喻与抽象总结混为同一规则；4 千字以上场景因此更容易误报，错误样本也会诱导 Agent 只改第一个无辜名词。

本批修复：

1. 抽象总结只检测明确的模板组合，不再把 `答案、真相、本身` 等可承担具体叙事功能的单词独立计罪；
2. 抽象总结按叙事单元和项目质量档案的 2% 软密度阈值判定，不再使用固定 8 次的长度偏置；
3. `像是` 归入既有比喻依赖规则，避免同一表达被两个规则重复归责，并让项目级 note/blocking 模式正常生效；
4. 新增具体抽象名词、`像是` 归属和高密度总结模板三类回归；64 项相关测试及架构审计通过；
5. 对真实第三场候选复算后仅剩项目档案定义的低级提示，不再有阻断项，未修改真实项目正文或降低项目自定义 Gate。

下一验收点：恢复同一运行，静态修订应通过正式门禁并进入状态、Canon 与连续性闭环。

### E2m 批次记录：反规避任务合同显式化

再次恢复后，模型把“不得旁白概述、不得机械转折、情绪由行动呈现”等通用写作要求复制成 `anti_evasion_rows`。这些行并不对应真实逐句修订，两个摘录也不属于精确源正文；Pi 又为修正字符串布尔和清单结构重写约 25 KB manifest，耗尽两轮 repair。根因是提示同时要求“必须建立反规避表”与“无风险时可为空”，却没有把确定性判定结果显式交给 Agent。

本批收敛：

1. Revision prompt manifest 新增受保护的 `anti_evasion_rows_required` 布尔值，由精确源正文的正式 lint 与项目质量档案计算；
2. Agent task 直接显示该值，并禁止把一般 requirements、全文合规检查、字数修复或未改动句子伪装成逐字证据；
3. `false` 时允许空表并由 Studio 写入受保护来源的不适用说明，`true` 时仍要求精确原句、修订句和批判性判断；
4. `yes/no/true/false/是/否` 等无歧义布尔传输由 Studio 归一化为 JSON boolean，不再浪费模型轮次修机械类型；
5. 行为回归 56 项和架构审计通过；新增逻辑已拆入小函数，未突破文件或函数复杂度棘轮。

下一验收点：用新 prompt 重新执行同一静态修订；输出必须只处理真实破折号问题，不再制造伪反规避行，并完成第三场闭环。

### E2n 批次记录：修订证据表去重与职责分类

显式合同复验后，Agent 正确发现了一条确定性正则未覆盖的隐性对照，证明 `anti_evasion_rows_required=false` 不能等同于禁止 Agent 补充语义发现；该判断应保留。剩余失败来自“重复谓语”修复误入反规避表，以及相同原句/修订句被重复登记。

本批增加窄范围的结构清理：

1. 含反规避、机械/显式转折、换皮、对照/对比等语义标记的 Agent 发现始终保留；
2. 只有明确属于字数、重复谓语、错别字、标点/比喻密度或抽象总结的行才从反规避表移出；
3. 相同 `source_excerpt + revised_excerpt` 的证据行确定性去重，保留首次文学判断；
4. 不按模糊文本相似度修正摘录，也不把 lint 未发现解释为 Agent 不得发现；
5. 53 项场景修订与预检回归、架构审计通过。

下一验收点：恢复原运行；误分类重复谓语行应被移出，隐性对照证据保留并通过 exact-evidence Gate。

### E2o 批次记录：候选正文语言门禁前移

`scene_0003` 的静态修订候选通过了 Studio 预检和候选晋升，但晋升后的静态审查又对同一正文检出
`dash-overuse`：11 个破折号约占 172 个叙事单元的 6.5%，超过项目 2% 阈值。根因不是审查过严，
而是候选预检只运行 Style Lint 与字数/读者合同，遗漏了自身合同已经声明的 Punctuation Lint；
同一确定性规则因此在事务写回前后产生不同结论。

本批按模块边界修复：

1. Engine 既有 `generation_gate` 增加 `candidate_language_gate`，聚合标点和 AI 文风的阻断证据；
2. Studio 通过既有 generation-gate 依赖消费稳定结果，不直接依赖标点或文风实现；
3. repair issue 分别保留标点重组和反规避修订语义，不能机械删标点、改变事实或用同义转折换皮；
4. 新增 Engine 聚合合同与 Studio 写回前阻断回归，覆盖破折号滥用和机械对照并存；
5. 67 项定向测试、编译、diff 检查和架构审计通过；没有新增 Studio 到 Engine 的依赖边，文件与函数预算均未突破基线。

下一验收点：重启开发服务并恢复原运行。当前静态修订必须在临时工作区内消除破折号超限，
通过同一语言门禁后才能再次晋升；晋升后的静态审查不得再因同一确定性问题回退。

### E2p 批次记录：编号修订产物事务化

E2o 复验中，主创 Agent 已把 11 处对白破折号改成规范引语，但 repair 仍失败：
`candidate-provenance-invalid` 被绑定到正文 `.md`，真正承载机器字段的 `_revision_02.json`
却被保护。Agent 无法修改 manifest，只能错误地把 `generated_by`、角色登记和读者合同写进正文头部。

根因是三处旧路径判断只支持无编号的 `_revision.md/.json`，而正式状态机在第二次修订起使用
`_revision_02`。本批修复：

1. `scene_revision_paths()` 成为编号修订四件套的唯一解析接口，由 canonicalization 与 validation 共用；
2. 修订 canonicalizer 现在能对 `_02/_03` manifest 写入 exact source、候选摘要、writer session、质量档案、读者/节奏合同和 provenance；
3. repair scope 识别版本化正文与 manifest，把二者作为一个事务写集，报告仍受保护；
4. validator 不再以旧后缀静默跳过编号修订合同；
5. 编号修订 canonicalization、相同正文拒绝和字数修复联动回归已覆盖；73 项定向测试、编译、diff 检查和架构审计通过。

下一验收点：重启并重新领取同一静态修订任务。正文不得再包含流程元数据；Studio 应在 JSON 中
确定性补齐机器字段，修订 Agent 只处理剩余语言问题，随后通过复核与晋升。

### E2q 批次记录：Pi Worker 阶段工具能力收缩

编号修订事务已正确生效：最新 repair context 同时包含 `_revision_02.md` 与 `_revision_02.json`，
Worker 也成功读取了两个完整目标。随后 DeepSeek 在协议已经进入写入阶段后仍连续请求
`read_authorized_source`；本地租约三次拒绝后触发 no-progress guard。根因不是沙箱缺文件，也不是旧任务
复用，而是 `tool_choice` 只作为供应商采样提示，发送给 `pi-messages` 的上下文仍声明全部七种工具，
兼容网关可能忽略精确选择。

本批按运行时接口修复：

1. 必需工具从“采样提示”升级为阶段性 capability lease；
2. `pi-messages` 在每次请求中只暴露当前阶段工具，OpenAI/Anthropic/Google/Bedrock 传输也同步收缩各自工具声明；
3. `beforeToolCall` 继续作为第二道失败闭锁，未授权工具仍不能执行；
4. 修复 profile 明确告知 Agent：全部目标读取完成后读取能力会撤销，必须一次写回完整事务；
5. 新增 `pi-messages` 与 OpenAI-compatible payload 回归，证明写入阶段不再向模型暴露读取工具；
6. Pi Worker 全量 48 项测试、TypeScript 构建、diff check 与架构审计通过，现有架构债务未增加。

下一验收点：恢复同一真实运行。repair 的第二次 provider 请求必须直接调用
`write_expected_output`，不得再出现读取租约拒绝；其后继续完成 `scene_0003` 后处理闭环。

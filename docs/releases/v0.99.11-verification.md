# ArcVellum v0.99.11 发布验证

## 本地变更与证据

- 新场景中，保存的作品级角色标签直接进入角色初始化；《文学少女》五名已建档人物均可读回，上一场三名参与者在新场景路径中各自获得专属标签。
- 将《文学少女》旧场的真实提示输入用于静态对照：原来源与推演共 20000 字符，新来源与交接共 17746 字符；节约 2254 字符。再计入本版真正注入的挂载文风约 1490 字符，该部分合计约 19236 字符，净减少约 764 字符（约 3.8%）。新摘录覆盖上一场结尾、最新方向、三名角色档案及世界规则；完整推演仍留在事务缓存中。此数字是字符量，不是模型实际账单 token。
- 定向测试：50 项通过，覆盖主创交接、角色标签初始化、提示合同、来源摘录、挂载文风和场景事务。
- 审查项目文风后确认：旧 ID `arcvellum-clear-plain-prose` 的当前内容是《弹性叙事》；原 lean-v2 初稿未注入挂载 `prompt.md`，只注入选段。本版已补主创初稿及修订的挂载上下文；更新模板后，《文学少女》通过正式版本构建和挂载服务切至 `v1-267cfd0f62c35e8264fd`，旧版未改写。

## 发布前检查

- [x] Python 全量回归：1628 项通过，1 项跳过。
- [x] Vue 客户端 79 个测试文件、257 项通过；生产构建及桌面资源同步通过。
- [x] Pi Worker 11 个测试文件、110 项通过，TypeScript 构建通过。
- [x] Prompt Registry 59 个资产、73 个任务 ID 有效；架构审计 0 循环、0 Studio 越界 Engine import；模块地图、七处版本声明与 `git diff --check` 通过。
- [x] [GitHub Actions 发布任务](https://github.com/o-1717986918/arcvellum/actions/runs/36159216681)成功：Windows、macOS Intel、macOS Apple Silicon 与 publish 均为 success；[公开 Release](https://github.com/o-1717986918/arcvellum/releases/tag/v0.99.11)含 Windows 安装包及签名、双架构 macOS 预览包、`latest.json` 和 `SHA256SUMS.txt`。

真实模型文学质量、Windows 安装版 GUI/更新实测与 macOS 签名公证不由上述结构化回归证明，作为发布后的独立验收边界保留。

# ArcVellum v0.99.3 发布验证记录

验证日期：2026-08-31

## 自动化结果

- Python：1308 项通过，1 项按既有平台条件跳过。
- Vue：66 个测试文件、199 项测试全部通过。
- Pi Worker：8 个测试文件、75 项测试全部通过。
- Prompt Registry：57 个提示词资产、72 个任务 Prompt ID，零错误、零警告。
- 架构审计：零新增依赖环、零 Studio -> Engine 反向依赖；既有债务未增加。
- 版本矩阵：Python、pyproject、Node、Pi Worker、Cargo、Tauri 均为 `0.99.3`。

## GitHub Release 生产资产

| 资产 | 大小 | SHA-256 |
| --- | ---: | --- |
| `ArcVellum_0.99.3_x64-setup.exe` | 52,878,486 bytes | `a59c44ea838810bac0ef8af30574673f2792c176034404fbc4528d488b165113` |
| `ArcVellum_0.99.3_x64-setup.exe.sig` | 420 bytes | `1ff608bff8ee3915f26ecba639eb5a3ad64bf48aa4189d8a666d44cfe98f0fbb` |
| `ArcVellum_0.99.3_macos-arm64_unsigned-preview.dmg` | 67,370,286 bytes | `daf2271958975df4e37a53009c4548850be29b90503e1d96f3868f60d475c274` |
| `ArcVellum_0.99.3_macos-x64_unsigned-preview.dmg` | 70,111,185 bytes | `ba3cbeefa98dfab09ef38922f33efe25154b0d55c632753876ebde449dc6bc98` |

构建完成 frozen Python sidecar、前端静态资源、Pi Worker、Node 22.19.0、只读演示项目、NSIS 安装器、updater 签名、`latest.json` 与 `SHA256SUMS.txt`。

## 安装后烟测

安装器以静默方式写入一个全新测试目录并通过以下检查：

- 安装目录包含 frozen Studio sidecar。
- 安装目录包含 `resources/pi-worker/node.exe` 与 `resources/pi-worker/dist/main.js`。
- Pi Worker 模型目录协议为 `arcvellum/pi-worker-catalog/v1`，运行时版本为 `0.99.3`，可识别 40 个 provider。
- 安装目录不包含 `opencode.exe`、OpenCode notice 或 license 资源。
- 安装后的 sidecar 可以独立启动并输出正式 CLI 命令面。

## macOS 状态

Apple Silicon 与 Intel 两条原生构建路线均已在 GitHub 的干净 macOS runner 上完成 PyInstaller sidecar、对应架构 Node runtime、Pi Worker、Tauri app 与 DMG 的完整装配，并成功上传 Release。两个 DMG 均明确命名为 `unsigned-preview`；它们没有冒充 Developer ID 签名与 Apple notarization 后的正式公证版本。

发布工作流：[ArcVellum desktop release #33347022582](https://github.com/o-1717986918/arcvellum/actions/runs/33347022582)。

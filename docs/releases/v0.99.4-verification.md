# ArcVellum v0.99.4 发布验证记录

验证日期：2026-08-31

## 自动化结果

- Python：1308 项通过，1 项因 Windows 不支持当前符号链接场景而按既有条件跳过。
- Vue：68 个测试文件、203 项测试全部通过。
- Pi Worker：9 个测试文件、80 项测试全部通过。
- Playwright：星仪 8 项、创作现场 1 项全部通过；100、300、1000 场景和 30,000 字符投影均有覆盖。
- Rust：`cargo check` 通过。
- 架构审计：16 个既有大文件债务、108 个既有复杂函数债务，零新增依赖环，零 Studio -> Engine 反向依赖。
- 版本矩阵：Python、pyproject、Node、Pi Worker、Cargo、Tauri 均为 `0.99.4`。

## Creative Live 闭环

确定性端到端测试以同一 `artifact_id` 与 exact digest 验证：

```text
streaming_preview
  -> candidate_written
  -> deterministic_preflight_passed
  -> semantic_review_passed
  -> revision_written
  -> mutation receipt
  -> promoted
```

SSE 测试覆盖浏览器 `Last-Event-ID` 续传、断线快照恢复、项目隔离与有界事件；前端验证 30,000 字符候选更新，Reader 不展示未晋升正文。

## 真实 Provider 证据

真实 Pi Worker 请求已经抵达用户配置的 DeepSeek Provider 与 `deepseek-v4-pro` 模型，Runtime、Provider 和模型路由均可确认。Provider 返回：

```text
402 Insufficient Balance
```

系统将其归类为 `provider-billing-required` 并安全暂停。该结果证明真实连接和错误分流可用；它不被记录为“真实模型完整文学闭环成功”。发布可复验的完整闭环证据来自确定性 Runtime/API/投影测试，外部账户恢复余额后可用同一路线补充真实文学生成证据。

## 生产资产

生产构建完成后在此记录 Windows 安装包、更新签名、macOS 预览包、SHA-256 与 GitHub Actions 运行地址。

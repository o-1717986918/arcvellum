# ArcVellum v0.99.1 发布验证记录

验证日期：2026-08-27

平台：Windows 11 x64

## 自动化验证

- Python：全量发现 1282 项。补齐发布文件前仅两个版本/发布元数据断言失败，两项修复后定向重跑通过；业务与架构测试无失败；1 项因当前 Windows 环境不支持测试 symlink 而跳过；
- Vue：65 个测试文件、200 项测试通过；
- Pi Worker：8 个测试文件、75 项测试通过；
- Prompt Registry：57 个 Prompt Asset、72 个任务提示词 ID，0 error、0 warning；
- Playwright：8 项通过，覆盖 100/300/1000 场景、SSE 增量投影、减动模式、顾问悬浮、平移缩放和左键旋转；
- OpenAPI：v2/v3/v4 叙事路由均存在，生成合同除版本从 0.99.0 更新到 0.99.1 外无行为漂移；
- Architecture Audit：16 项受控历史文件债务、110 项受控历史函数债务、0 新违规、0 循环、0 Studio 对 Engine internal 的导入；
- 版本同步、兼容面审计、module map check、`git diff --check` 均通过。

## 生产构建

- Vue：2601 modules，94 个同步前端资产，2 个 WebP 视觉资产，桌面资源同步通过；
- 星仪主 chunk：513.65 kB（gzip 161.90 kB），构建通过但保留为下一阶段 Orrery 模块内懒加载债务；
- Python sidecar：来源 SHA-256 `477af98fa3307257301c5bd9ca3d86d4bfe0f84b16b1d277bbb6202e7a71b4ca`；二进制 SHA-256 `6e25035dd336c842ea4f730835cd5745ee965db9510306283f3f6d30a3fc4225`；
- Pi Worker：Node 22.19.0；7 个白名单文件；bundle SHA-256 `01659491956cbc3303221133c785be6498e2b58dffde2c73dc4ff0f5a6fecc44`；
- Rust/Tauri release profile 编译通过；NSIS 安装包和 updater 签名生成成功。

## 生产产物

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `ArcVellum_0.99.1_x64-setup.exe` | 127,043,423 bytes | `819dc7e2dcc8f6053e9232cb0f671c23322eb92bee8022a6786c2e1430499271` |
| `ArcVellum_0.99.1_x64-setup.exe.sig` | 420 bytes | `ddd83f6b2dbc38ab5a40bb993470133ffb29251f711cc447404cedac63822d1b` |

`latest.json` 已引用同版 NSIS 文件和非空 updater 签名；`SHA256SUMS.txt` 由构建脚本重新生成，没有夹带旧版安装包。

## 安装与桌面冒烟

1. NSIS 静默安装到新的隔离目录成功，退出码为 0；
2. 桌面主程序、冻结 sidecar、OpenCode、OpenCode 安装收据、Pi Worker Node/entrypoint/安装收据均存在；
3. 安装目录内 sidecar 在 OS 分配端口发布 nonce 绑定的 `arcvellum-sidecar/v1` ready-file；鉴权 `/health` 返回 `ok=true`、`version=0.99.1`；
4. 同版本原位修复安装成功，7 项必需资源仍完整；
5. 真实 Tauri 壳启动后产生新的 sidecar ready-file；主窗口发出关闭请求后，安装目录内 sidecar 进程数量回到 0，没有遗留服务。

## 模块化就绪与残余风险

- 面向接口开发准备通过，正式判定见 `docs/architecture/reviews/v0991-modular-development-readiness-audit.md`；
- 1000 场景视觉验收通过，单项耗时约 46.3 秒；API 投影约 0.37 秒，主要性能债务位于浏览器首帧和 Orrery 渲染，不在文学 Engine；
- 尚未在独立 Windows 虚拟机完成从旧签名版本升级、离线冷启动和完整卸载；GitHub Actions 将从正式 tag 再运行一次干净环境全套构建。

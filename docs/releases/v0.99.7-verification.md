# ArcVellum v0.99.7 发布验证

验证日期：2026-09-20。

## 本地检查

- 版本同步：Python、Node、Pi Worker、Cargo、Tauri 均为 `0.99.7`。
- Python：1449 项测试完成，1 项因本机 Windows 符号链接环境跳过，其余通过。使用本仓库 `.venv` 验证；系统 Python 仍指向另一份旧工作树，不作为发布证据。
- Vue：69 个测试文件、224 项测试通过；TypeScript 检查与生产前端构建通过。
- Pi Worker：10 个测试文件、102 项测试通过。
- Playwright：10 项浏览器端验收通过，覆盖会话归属、创作现场、星仪导航及大量节点。
- Prompt Registry：59 个资产、73 个任务标识通过；17 个确定性提示词评估用例通过。在线模型语义评估未运行。
- 架构审计、模块地图与版本同步检查通过。

## 发行构建

[GitHub Actions 发布工作流](https://github.com/o-1717986918/arcvellum/actions/runs/35500650812) 全部通过，Windows、macOS Apple Silicon、macOS Intel 和发布作业均成功。[GitHub Release](https://github.com/o-1717986918/arcvellum/releases/tag/v0.99.7) 已上传 6 个附件：Windows NSIS 安装器、更新签名、`latest.json`、`SHA256SUMS.txt` 及两种 macOS 预览包。`releases/latest` 指向 `v0.99.7`；更新清单版本为 `0.99.7`，指向该版本安装器。

Windows 安装器 `ArcVellum_0.99.7_x64-setup.exe` 为 52,766,201 字节，发布校验表中的 SHA-256 为 `2cb2cee3e226630d314dc2a26d1f4471dc7f3ef7faa7591bf78f03f0fc17c999`。macOS 预览包未经过 Apple Developer ID 签名与 notarization。

此次没有在干净的 Windows/macOS 机器上完成首次安装、覆盖升级与应用内更新的实机验收；CI 构建成功不等同于这些体验已验证。工作流还提示所用 `actions/*@v4/v5` 依赖的 Node 20 正在弃用，需在后续维护中升级 Actions 版本。

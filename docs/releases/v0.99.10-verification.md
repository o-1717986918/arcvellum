# ArcVellum v0.99.10 发布验证

## 发布变更包

```yaml
module_change_packet:
  objective: "发布资产初始化、事件预算、创作语言与 Agent 控制修正"
  primary_module: "packaging/"
  public_entry: "v0.99.10 Git 标签、Release 资产和 Tauri updater latest.json"
  variation_point: "none；沿用既有标签工作流和签名构建"
  inputs: ["已验证的修复提交", "各平台版本声明", "发布说明"]
  outputs: ["Windows 签名安装/更新包", "macOS 未签名预览包", "签名清单与校验和"]
  invariants: ["版本声明一致", "不新增文学门禁", "不输出签名私钥", "不修改用户作品"]
  allowed_dependencies: ["版本同步脚本", "既有 CI 发布工作流", "sidecar/Pi Worker provenance"]
  forbidden_dependencies: ["手工伪造更新签名", "覆盖既有标签", "绕过 CI 失败宣称发布"]
  tests: ["版本同步", "Python/Vue/Pi Worker 回归", "架构与提示词校验", "Release 资产签名和 SHA256"]
  rollback_unit: "独立版本提交与标签"
  documentation: ["发布说明", "本验证记录", "README 当前版本"]
```

## 真实作品验收

- [x] 《青春之旅》六份人物档案与世界规则完整落盘；6 条规则、5 项约束、3 个待确认问题可读，未留下半套资产。
- [x] 《他与她》前 70 场合同、正文和提交凭据 SHA-256 均未改变；正式计划为 86 场，未来 16 场按 4/6/6 分配到后三章。
- [x] 《他与她》总字符目标保持 300000，作品契约、计划摘要、字数预算和节奏计划中的场数均同步为 86。
- [x] 两个历史运行均未自动恢复；修复动作不会绕过用户决定或重放陈旧候选。

## 本地验证

- [x] Python 全量 1511 项通过（1 项跳过）。
- [x] Vue 75 个文件、248 项通过；生产前端构建与桌面资源同步通过。
- [x] Pi Worker 10 个文件、103 项通过。
- [x] Python、Node、Pi Worker、Cargo 与 Tauri 七处版本声明均为 `0.99.10`；Cargo metadata、架构审计、模块映射、59 个提示资产、73 个任务标识与 17 项确定性提示词评估均通过。在线模型语义评估未运行。
- [ ] Windows/macOS 标签构建、更新签名、校验和与 GitHub Release 附件。

## 尚未冒充完成的验证

- [ ] 更新后的 Windows 安装版 GUI 首次安装、覆盖升级、星仪读取、SSE、退出回收与应用内自动更新。
- [ ] macOS Developer ID 签名和 Apple notarization；当前仍只发布未签名预览包。
- [ ] 跨题材、同情节、同模型的盲评 A/B。当前能确认提示词和生成链路已经采用新规则，不能据此宣称所有成稿文风都已达标。
